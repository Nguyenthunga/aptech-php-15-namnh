{{-- @foreach($users as $user)
<div>
  {{$user->name}} - {{$user->passport_id}} - {{$user->passport->number}}
</div>
@endforeach --}}
<!-- print data with array -->
{{-- @foreach($users as $user)
<div>
  {{$user['id']}} - {{$user['name']}} - {{$user['passport']['number']}} </div>@endforeach --}}
